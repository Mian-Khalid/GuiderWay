@model List<MvcApplication1.Models.Scholarship>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div style="margin-left: 250px; margin-top: 100px">
    <h2 style="font-size: xx-large; color: white; margin-right: 500px;">Scholarships</h2>


  
    <script>
        function f123(path)
        {
            // $("#datail123").append(path);
            
            //$("#datail123").append(id);
            $.getJSON("/Scholarship/getDatail?Id=" +path, function (data) 
            {
                
                if (data) 
                {
                    $( "#datail123" ).load( data.DetailLink );
                    // alert(data.DetailLink);
                    $.each(data, function (i, item)
                    {
                        
                    
                        $("#datail123").append(data.DatailLink);

                        // $('#userName').val("");
                        // alert("jhjhjhj");
                        return false;
                    });
                }
                else {
                    // $('#result').text("User Name is available");
                    return true;;

                }
            });
             
            //var fso = new ActiveXObject('Scripting.FileSystemObject'),
            //    iStream=fso.OpenTextFile(path, 1, false);
            //while(!iStream.AtEndOfStream)
            //{
            //    document.body.innerHTML += iStream.ReadLine() + '<br/>';
            //}        
            //iStream.Close();
            $(function(){
                $( "#datail123" ).load( path );
            });

        }
        
        
    </script>
    <table border="10">
        <tr>
            <th>Level
            </th>
            <th>Amount
            </th>
            <th>Institute
            </th>
            <th>Detail
            </th>
           <th>
               GetDetail
            </th>

        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.leval
                </td>
                <td>
                    @item.Amount
                </td>
                <td>
                    @item.Institute
                </td>
                <td>
                    @item.Detail
                </td>
                <td>
                    @*<a href="~/Student/Edit/@item.Id">Edit</a>*@

                    <input type="button" value="Detail" onclick="f123(@item.Id);">

                    @* @Html.ActionLink("Edit", "ProgramUpdate", new { id=item.Id }) *@
                    @*  @Html.ActionLink("Details", "", new { id=item.Id }) 
            @Html.ActionLink("Delete", "ProgramDelete", new { id=item.Id })*@
                </td>
            </tr>
    
        }

    </table>
</div>
<div id="datail123" style="margin-left: 80px; margin-right: 80px; color: greenyellow; border: solid; font-size: larger;">
</div>
